# 🌶 Перчик

Телеграм-бот для сбора статистики по сменам. Читает чеки из топика форума
(включая статьи / Rich Messages), складывает в SQLite и отдаёт сводку
с графиками по инлайн-кнопкам.

## Возможности

Парсит чеки в свободной форме: дата и время смены, доход по каждому сайту,
токены, лайки, счётчик подписчиков. Считает статистику за произвольный период,
разбивку по площадкам, прирост подписчиков (как дельту между снимками),
рисует графики дохода и роста аудитории.

## Установка

```bash
python -m venv .venv
.venv\Scripts\activate        # Windows
pip install -r requirements.txt
cp .env.example .env          # затем заполнить
python bot.py
